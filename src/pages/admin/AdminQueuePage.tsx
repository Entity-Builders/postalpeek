/**
 * AdminQueuePage.tsx — /admin/queue
 * Wrapper around AdminQueue component.
 */

import React from 'react';
import { AdminQueue } from '../../components/AdminQueue';

export function AdminQueuePage() {
  return <AdminQueue />;
}
